
```
print(default_api.natural_language_write_file(language = "typescript", path = "components/business-ads/CreateAdForm.tsx", prompt = "In typescript, Add `targetedCourses`, `targetedUniversities`, `targetedYearOfStudies` to the form and add it to the request made when the form is submitted.."))
```
